# Normalize line endings to LF in the repo (avoids CRLF churn across Windows/macOS/Linux)
* text=auto eol=lf

# Binary assets — never touch line endings
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.woff binary
*.woff2 binary
